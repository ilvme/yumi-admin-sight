// 在此定义密码加密字段
export const PASSWORD_KEYS = ['password', 'pwd', 'dbaPwd', 'dbPwd', 'newPwd', 'newPassword']

// 密码加密方法
export const encrypt = (value) => {
  return value
}

// 解密
export const decrypt = (value) => {
  return value
}
